# NOM DU SCRIPT : requirements.txt
# ROLE          : Liste des dépendances Python du back-end
# DESCRIPTION   : Déclare les paquets nécessaires à l'API Flask et à son déploiement sur Render :
#                 Flask (framework web), flask-cors (autorise les requêtes cross-origin depuis le
#                 front-end statique), gunicorn (serveur WSGI de production) et gdown (téléchargement
#                 de fichiers, ex. bdd.db, depuis Google Drive/GitHub)
# VERSION       : 1.0
# LICENCE       : réalisation de programme - requirements.txt
# USAGE         : pip install -r requirements.txt
# AUTEUR        : Maël Khonde Mbumba


Flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
gdown==5.2.0
